const UPLOAD_USER_IMAGE_FILE = "public/images/users";

const MAX_FILE_SIZE = 1024 * 1024 * 2;

const ALLOWED_FILE_TYPE = ["jpg", "jpeg", "png", "gif"];

module.exports = {
  UPLOAD_USER_IMAGE_FILE,
  MAX_FILE_SIZE,
  ALLOWED_FILE_TYPE,
};
